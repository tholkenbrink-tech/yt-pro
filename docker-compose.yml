services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  api:
    image: ghcr.io/tholkenbrink-tech/yt-pro-api:latest
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////data/database/app.db
      REDIS_URL: redis://redis:6379/0
      TEMP_DIR: /data/temp
      THUMBNAIL_DIR: /data/thumbnails
      COOKIE_DIR: /data/cookies
    volumes:
      - /volume1/media/yt-pro/database:/data/database
      - /volume1/media/yt-pro/temp:/data/temp
      - /volume1/media/yt-pro/thumbnails:/data/thumbnails
      - /volume1/media/yt-pro/cookies:/data/cookies
    depends_on:
      redis:
        condition: service_healthy
    command: >
      sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    ports:
      - "127.0.0.1:8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3

  worker:
    image: ghcr.io/tholkenbrink-tech/yt-pro-api:latest
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////data/database/app.db
      REDIS_URL: redis://redis:6379/0
      TEMP_DIR: /data/temp
      THUMBNAIL_DIR: /data/thumbnails
      COOKIE_DIR: /data/cookies
      PYTHONPATH: /app
    volumes:
      - /volume1/media/yt-pro/database:/data/database
      - /volume1/media/yt-pro/temp:/data/temp
      - /volume1/media/yt-pro/thumbnails:/data/thumbnails
      - /volume1/media/yt-pro/cookies:/data/cookies
    depends_on:
      api:
        condition: service_healthy
    command: ["python", "worker/run.py"]
    healthcheck:
      disable: true

  scheduler:
    image: ghcr.io/tholkenbrink-tech/yt-pro-api:latest
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////data/database/app.db
      REDIS_URL: redis://redis:6379/0
      TEMP_DIR: /data/temp
      THUMBNAIL_DIR: /data/thumbnails
      COOKIE_DIR: /data/cookies
      PYTHONPATH: /app
    volumes:
      - /volume1/media/yt-pro/database:/data/database
      - /volume1/media/yt-pro/temp:/data/temp
      - /volume1/media/yt-pro/thumbnails:/data/thumbnails
      - /volume1/media/yt-pro/cookies:/data/cookies
    depends_on:
      api:
        condition: service_healthy
    command: ["python", "scheduler/run.py"]
    healthcheck:
      disable: true

  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      api:
        condition: service_healthy

volumes:
  redis_data:
